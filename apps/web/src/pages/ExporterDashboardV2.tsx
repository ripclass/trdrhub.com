// ExporterDashboardV2 currently reuses the proven legacy implementation.
// This ensures feature parity while we rebuild the new layout safely.
import ExporterDashboard from "./ExporterDashboard";

export default ExporterDashboard;
