# LCopilot API Environment Configuration

# Database Configuration
DATABASE_URL=postgresql://postgres:password@localhost:5432/lcopilot

# AWS Configuration (only needed when USE_STUBS=false)
AWS_REGION=us-east-1
S3_BUCKET_NAME=lcopilot-documents

# Google Cloud Configuration (only needed when USE_STUBS=false)
GOOGLE_CLOUD_PROJECT=your-gcp-project
GOOGLE_DOCUMENTAI_LOCATION=us
GOOGLE_DOCUMENTAI_PROCESSOR_ID=your-processor-id

# Authentication
SECRET_KEY=your-secret-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Stub Mode Configuration
# Set to true for local development without real AWS/GCP credentials
USE_STUBS=true
STUB_SCENARIO=lc_happy.json
STUB_FAIL_OCR=false
STUB_FAIL_STORAGE=false
STUB_DATA_DIR=./stubs
STUB_UPLOAD_DIR=/tmp/lcopilot_uploads

# Application Settings
DEBUG=false

# Available Stub Scenarios:
# - lc_happy.json: Perfect validation with no discrepancies
# - lc_mismatch.json: Multiple Fatal Four violations (dates, amounts, parties, ports)
# - lc_expired.json: Expired LC scenario (critical validation failure)
#
# Error Simulation:
# - STUB_FAIL_OCR=true: Simulates OCR processing failures
# - STUB_FAIL_STORAGE=true: Simulates S3 storage failures