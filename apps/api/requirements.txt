fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
alembic==1.12.1
psycopg2-binary==2.9.9
python-multipart==0.0.6
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt>=4.1.2  # Required for passlib bcrypt_sha256 support (must have __about__ attribute)
python-dotenv==1.0.0
pydantic==2.5.0
pydantic-settings>=2.0.3
supabase>=2.9.0  # Updated to fix proxy parameter issue
httpx>=0.27.0,<0.28.0  # Compatible with supabase-py 2.9.0+
aiohttp>=3.9.0  # For async HTTP requests in notification service
markdown>=3.5.0  # For markdown processing in collaboration service
bleach>=6.0.0  # For HTML sanitization in collaboration service
prometheus-client>=0.19.0  # For Prometheus metrics
requests>=2.31.0
redis==5.0.1
celery==5.3.4
pytest==7.4.3
pytest-asyncio>=0.23.5
pytest-cov>=5.0.0
structlog==24.1.0
psutil==5.9.8
mangum>=0.17.0  # AWS Lambda adapter (optional)
werkzeug>=3.0.0
boto3>=1.34.0
email-validator>=2.0.0
PyJWT>=2.8.0
weasyprint>=61.0
openpyxl>=3.1.5
stripe>=7.0.0
# Google Cloud Document AI OCR (upgraded Dec 2025 - fix processor_version field)
google-cloud-documentai>=2.29.0  # Latest as of Dec 2025
google-api-core>=2.19.0  # Required by documentai
grpcio>=1.62.0  # Required for gRPC communication
proto-plus>=1.25.0  # Required for protobuf handling - must be recent for new API fields
protobuf>=4.25.0  # Explicit protobuf version for field compatibility
google-cloud-core>=2.4.1  # Core Google Cloud client library

# LLM Provider dependencies (for AI enrichment)
openai>=1.0.0  # OpenAI GPT provider
anthropic>=0.18.0  # Anthropic Claude provider

# DeepSeek OCR dependencies (optional - only needed if USE_DEEPSEEK_OCR=true)
# Commented out to reduce build size (~3.5GB). Uncomment if enabling DeepSeek OCR.
# torch>=2.0.0
# transformers>=4.35.0
pillow>=10.0.0  # Used by other parts of the app
pdf2image>=1.16.0  # Used by other parts of the app
pdfminer.six>=20231228
PyPDF2>=3.0.1
