# Regular package marker: a globally-installed third-party "scripts" package
# (Kestrel engine, pip -e) shadows namespace resolution of this directory —
# regular packages beat namespace packages regardless of sys.path order.
